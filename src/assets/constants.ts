import { generateCells, generateRegionDict } from '@/utils/generateDicts';

export const BOARD_SIZE = 9;
export const BOX_SIZE = 3;

export const CELLS = generateCells();
export const REGION_DICT = generateRegionDict();
